import { MicroWindow } from '../types'

export const originalWindow: MicroWindow = window
export const originalDocument: Document = document
export const originalQuerySelector = document.querySelector

